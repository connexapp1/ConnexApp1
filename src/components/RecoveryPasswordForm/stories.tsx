import { Story, Meta } from '@storybook/react/types-6-0'
import RecoveryPasswordForm from '.'

export default {
  title: 'RecoveryPasswordForm',
  component: RecoveryPasswordForm
} as Meta

export const Default: Story = () => <RecoveryPasswordForm />
