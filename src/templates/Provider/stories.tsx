import { Story, Meta } from '@storybook/react/types-6-0'
import ProviderForm from '.'

export default {
  title: 'ProviderForm',
  component: ProviderForm
} as Meta

export const Default: Story = () => <ProviderForm />
