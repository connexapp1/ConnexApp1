import { Story, Meta } from '@storybook/react/types-6-0'
import RecoveryPassword from '.'

export default {
  title: 'RecoveryPassword',
  component: RecoveryPassword
} as Meta

export const Default: Story = () => <RecoveryPassword />
